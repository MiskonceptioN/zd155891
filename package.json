{
  "name": "website",
  "version": "1.1.0",
  "description": "Standard Resume public website.",
  "private": true,
  "scripts": {
    "visual-tests": "percy exec --config=visual-tests/.percy.json -- node visual-tests/snapshots.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@percy/cli": "^1.0.0-beta.58",
    "@percy/puppeteer": "^2.0.0",
    "puppeteer": "^13.7.0"
  }
}
